const myPerson = {
    name: 'John', 
    address: 'Jerusalem',
    age: 45
}

const {name, age} = myPerson;
console.log(`name = ${name}, age = ${age}`); // name = John, age = 45

// in react-native we used: `const { id } = route;`


הפסקה עד 10:20

